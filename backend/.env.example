# Copy to .env and fill in. Used by uvicorn when loading backend/main.py
ANTHROPIC_API_KEY=
# Optional — override if your account uses a different Messages API model id
# ANTHROPIC_MODEL=claude-sonnet-4-6
